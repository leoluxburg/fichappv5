<br>
<div class="container">
  <div class="jumbotron" style="background-color: white;">
    <div class="">
      <div class="text-center">
       <img src="https://pbs.twimg.com/profile_images/1201996982224670725/n8DvaV61_400x400.jpg" alt="municipiochepo" alt=""height="180">
       <br>
       <br>
       <h1><i class="fas fa-mountain"></i></h1>
       <h2>FICHAS RURALES</h2>
     </div>
    <br>
    <div class="text-center">
        <form action="/rurals" method="get">
          <div class="input-group">
            <input type="text" class="form-control" size="50" placeholder="Numero de Lote" name="search" id="search">
            <div class="input-group-btn">
              <button class="btn btn-fichapp" type="submit" value="Search" data-disable-with="Search" name="commit">Buscar</button>
            </div>
          </div>
        </form>
      </div>
    <br>
     <div class="table-responsive">
      <table class="table">
        <thead>
          <tr>
            <th scope="col">Numero de Lote</th>
            <th scope="col"><i class="fas fa-eye"></i></th>
            <th scope="col"><i class="fas fa-pencil-alt"></i></th>
            <th scope="col"><i class="fas fa-trash-alt"></i></th>
          </tr>
        </thead>

        <tbody>
          <% @rurals.each do |rural| %>
          <tr>
            <th scope="row"><%= rural.n_predio %></th>
            <td style="border-color: white;"><%= link_to 'Ver', rural %></td>
            <td style="border-color: white;"><%= link_to 'Editar', edit_rural_path(rural) %></td>
            <td style="border-color: white;"><%= link_to 'Eliminar', rural, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <br>
    <div class="text-center">
      <% if current_user.role === "Inspector" %>
      <p></p>
      <% else %>
      <%= link_to 'Registar', new_rural_path, class: 'btn btn-fichapp' %>
      <% end %>
    </div>
  </div>
</div>
</div>
<br>
<br>
